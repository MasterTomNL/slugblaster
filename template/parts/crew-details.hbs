<section class="crew-details flexrow">
  <div class="actor-img">
    <img class="profile-img flex0" src="{{actor.img}}" data-edit="img" title="{{actor.name}}" height="100" width="100" />
  </div>
  <div class="crew-details">
    <label for="name">
      {{ localize 'SB.name' }}
      {{#if editable }}
      <a class="rollableTable" title="{{ localize 'SB.randomize' }}" data-action="crew"><i class="icon icon-dice"></i></a>
      {{/if}}
    </label>
    <input id="name" name="name" type="text" value="{{actor.name}}" placeholder="{{ localize 'SB.ph_name'}}" />
    
    <div>
      <label for="brands">
        {{ localize 'SB.brands' }}
        {{#if editable }}
        <a class="rollableTable" title="{{ localize 'SB.randomize' }}" data-action="brands"><i class="icon icon-dice"></i></a>
        {{/if}}
      </label>
      <input id="brands" type="text" name="system.brands" value="{{actor.system.brands}}" placeholder="{{ localize 'SB.ph_brands' }}" />
    </div>
    
    <div>
      <label for="hangouts">
        {{ localize 'SB.hangouts' }}
        {{#if editable }}
        <a class="rollableTable" title="{{ localize 'SB.randomize' }}" data-action="hangouts"><i class="icon icon-dice"></i></a>
        {{/if}}
      </label>
      <input id="hangouts" type="text" name="system.hangouts" value="{{actor.system.hangouts}}" placeholder="{{ localize 'SB.ph_hangouts' }}" />
    </div>
    
    
    <h3>{{ localize 'SB.fractures' }}</h3>
    <div class="crewFractures">
      <input type="text" name="system.fracture_1" value="{{actor.system.fracture_1}}" placeholder="{{ localize 'SB.ph_fracture' }}" />
      <input type="text" name="system.fracture_2" value="{{actor.system.fracture_2}}" placeholder="{{ localize 'SB.ph_fracture' }}" />
    </div>
  </div>

</section>
